# v0.9.1 焦点渐变修复

- 聚焦档保留 DWM 材质和透明界面，焦点事件仅更新状态文字，不再重设背景、边框扩展或强制活动外观。系统继续处理原生焦点渐变。
- 关闭、保持、轻量视觉与高对比度策略保留。
- Release 编译、JavaScript 语法、24 种材质配置组合与设置模拟检查通过；新增失焦保留透明表面和迟到状态回包的回归检查。
- 未启动或操控播放器；实际系统渐变、保持档和模式切换效果由用户手动验证。

# v0.9.0 验证记录

验证日期：2026-09-19。按用户要求，未启动或操控播放器。

- Release 发布与 JavaScript 语法检查通过。
- 原生纯逻辑 96 种组合：三档、焦点、轻量视觉、系统支持、高对比度、保持接口可用性；非法档位回退检查通过。检查程序不创建窗口，不调用 DWM。
- 设置模拟检查：旧布尔值迁移、新选择优先、保存与重新载入、原生焦点事件、轻量视觉保留档位、迟到回包、快速切档、系统不支持时禁用以及保持失败提示。
- 未验证：本机 DWM 是否持续渲染失焦玻璃、Alt+Tab 外观、窗口模式切换、原生对话框、多屏 DPI、系统透明/节能策略。接口返回成功不等于材质已目视确认。

可复现检查：`node scripts/test-glass-modes.cjs`、PowerShell 执行 `scripts/test-glass-policy.ps1`。

手动试用建议：依次切三档并点击其他应用；在“保持”下开关轻量视觉；重启检查选择保留；分别在正常、小窗、沉浸模式尝试失焦。确认其他应用能正常获得焦点。

# v0.8.0 验证记录

验证日期：2026-09-19。按用户要求，未启动或操控播放器，也未播放测试音频。

- Release 发布与 JavaScript 语法检查通过。
- 22 项音频引擎模拟检查：双路交接、等功率曲线、统一音量/静音、暂停与恢复、进度定位、加载期间取消、失败保留原曲、过期目标、短切歌、音频时钟完成、复用音频源、短曲限制、释放写标签文件、异步恢复取消、快速连切、普通播放回退与计时器释放。
- 20 项播放器队列调度检查：预加载不改变当前歌/队列、触发时间、历史、随机队列消费、关闭/暂停/单曲循环/交接期间抑制、列表循环、缺失文件、手动短过渡、短曲与未知时长、连续时钟回调去重、队列变动使旧请求失效。
- 现有队列整理与封面像素检查通过。
- 模拟接口无法证明真实解码、可听无间断、压缩器听感、后台时序、悬浮歌词与视觉同步；这些需要手动试用。

可复现的纯逻辑检查：`node scripts/test-audio-engine.cjs`、`node scripts/test-transitions.cjs`、`node scripts/test-personalization.cjs`。

# v0.7.0 验证记录

验证日期：2026-09-19。按用户要求，未启动或操控播放器。

- Release 发布、JavaScript 语法检查通过。
- 原生歌词纯逻辑与屏幕边界共 13 项：开始前、当前与下句、双语、回退定位、末句、重叠字幕、字幕失效与间隔、空列表、同时间行、负坐标显示器、离屏恢复。检查程序未创建窗口。
- 队列模拟检查：实际下一首选择、随机播放下优先下一首、添加去重、移除保留曲库、保护当前歌、向前/后排序、排序退出随机、清空其他、缺失文件拒绝。
- 封面像素检查：黑白回退、透明像素过滤、红/蓝主色与浅色高光。
- 悬浮窗口显示、透明边缘、锁定后的实际鼠标穿透、最小化同步、跨显示器 DPI、真实封面效果与拖动交互留待用户手动试用。

# v0.6.0 验证记录

验证日期：2026-09-19。按用户要求未启动或控制播放器。

- Release 发布与 JavaScript 语法检查通过。
- 6 项模拟桥接与动画接口检查通过：三模式往返、进入途中退出、连续请求合并、原生错误后恢复交互、减少动态效果、轻量视觉。
- 模拟检查不涉及实际 Windows 窗口、DWM 或音频输出。
- 待手动试用：正常/小窗/全屏的动画，全屏覆盖任务栏，Esc 返回原位置，最大化还原，Alt+Tab，多显示器与不同 DPI，播放连续性。

# v0.5.0 动态频谱更新

日期：2026-09-19。

- 新增侧栏与沉浸模式的 32 段 Canvas 频谱，频率数据来自实际音频源。
- JavaScript 语法检查、Release 发布通过。
- 使用模拟的 Web Audio / Canvas / requestAnimationFrame 接口完成代码逻辑检查：并发初始化与切歌复用同一个音频源、只有一条可听输出、频率数据驱动柱高、暂停回落并停止循环、轻量视觉/隐藏页面/开关暂停绘制。
- 模拟接口不验证 WebView2 的真实解码、跨域音频分析、音量/立体声输出或最终布局。按用户要求未启动或控制播放器，真实播放与视觉测试待用户试用。
- 下方是历史版本结果，不能视为 v0.5.0 的运行测试。

# v0.4.1 歌词拖入更新

日期：2026-09-17。

- 歌词显示区接收单个 LRC / SRT / JSON / TXT 文件，沿用现有解析、验证和保存流程。
- 根据松手时的当前歌曲确定关联；加入文件数量、文件类型、未选择歌曲和重复导入的提示。
- 拖动时歌词区显示目标歌曲，并隐藏全窗口的音乐导入遮罩；拖离、取消和松手后清除提示。
- 完成 JavaScript 语法检查及 Release 编译。按用户要求没有启动或控制播放器；资源管理器真实拖放与播放同步留给用户试用。
- 下方是历史版本验证记录，不代表本版已进行相同的界面测试。

# v0.4.0 JSON / SRT 更新

- 扩展原生文件选择器、歌词解析和时间段高亮；在保存前验证歌词结构，导入失败保留旧歌词。
- 完成 Release 编译和 JavaScript 语法检查，以及无界面的基础格式检查。
- 按用户要求，本版未启动或控制播放器，没有进行原生文件窗口、界面布局和实际播放同步测试；这些操作留给用户试用。
- 下方 v0.3.0 的运行测试是历史结果，不代表 v0.4.0 已完成相同测试。

# v0.3.0 歌词更新验证

日期：2026-09-16；本机 Windows 11 x64，150% 显示缩放。

- Release 发布：0 错误、0 警告；JavaScript 语法检查通过。
- [13 项歌词解析检查](lyrics-parser-test.json)：精度、排序、多标签、双语去重、偏移、间奏、边界定位、TXT 原文、安全文本与数量限制。
- [19 项曲库与文件检查](self-test-v0.3.0.json)：包括 UTF-8 / UTF-16 / GB18030，歌词持久化，移走原歌词仍可读取，空文件/超大文件拒绝且保留旧歌词，原音乐未改动。
- [46 项真实 WebView2 检查](ui-test-v0.3.0.json)：包含原有播放、编辑、玻璃与迷你窗口回归，以及 LRC 导入、双语合并、定位高亮、点击跳转、自动滚动、切歌清除/恢复、TXT 替换、侧栏与沉浸窗口布局。
- 通过原生文件选择器实际导入 TXT，确认提示、文件名和正文更新成功。
- 检查了真实播放器界面与歌词截图；WebView2 截图本身不包含系统桌面合成层。
- 原位置升级前后，用户 library.json 的 SHA-256 保持一致；现有曲库与界面设置目录保留。
- 自动测试仅使用独立测试曲库和原创文字。

以下保留此前验证记录和兼容性限制。

# v0.2.0 验证记录

验证日期：2026-09-16。环境：Windows 11 x64，.NET 10.0.11，WebView2 Runtime 152.0.4191.66，显示缩放 150%。

## 桌面玻璃更新

- 36 项原生 WebView2 检查全部通过，见 [v0.2.0 集成结果](ui-test-v0.2.0.json)。
- Windows DWM 确认 Acrylic 背景类型为 3；系统 API 和扩展玻璃框架调用成功。
- 验证玻璃开启/关闭、恢复实底、背景染色 0% 与 60%、轻量模式暂停玻璃、设置窗口边界。
- 窗口整体 Opacity 保持 1，未使用整窗透明度淡化文字或封面。
- 使用单独的原生测试窗口作为后景，每 5 秒切换青橙与红蓝色块。通过桌面窗口截图确认前景播放器随背景同步变化，证明透视来自桌面合成。
- 主界面、迷你模式和沉浸模式共用同一桌面玻璃宿主；播放与编辑回归检查通过。
- 更新原位置的程序时，曲库 library.json 的 SHA-256 保持一致。未迁移或重建用户曲库。

## 基础检查与上一版结果

- Release 构建及框架依赖发布：0 错误，0 警告。
- JavaScript 语法检查。
- 11 项文件与曲库检查，结果见 [self-test.json](self-test.json)：WAV 元数据读取、导入去重、编辑不改动源文件、曲库重载、写回备份的 SHA-256 一致性、中文标签写回、时长保持、无效文件错误、分段音频流读取与定位、损坏曲库保留备份并提示。
- 29 项真实 WebView2 集成检查，结果见 [ui-test.json](ui-test.json)：原生消息桥、示例导入与去重、封面提取、普通与尾部字节范围请求、越界请求、播放时钟推进、暂停、切歌、定位、收藏、中文及 HTML 特殊字符安全显示、元数据重载、歌单去重、搜索空状态、专辑视图、轻量视觉开关、编辑表单提交、迷你窗口切换和沉浸模式。
- 使用原创 WAV 转换生成 MP3、FLAC、M4A/AAC 文件；在原生播放器中逐一确认播放时钟推进与进度定位。
- [preview.png](preview.png) 保留为原版深色模式布局截图。WebView2 自身的 CapturePreview 不包含 Windows 桌面合成层，不能用该类截图证明系统玻璃；v0.2.0 的透视效果另用实际桌面截图人工核验。

## 未验证 / 限制

- 不将播放时钟验证等同于主观音质评测。未测 WASAPI 独占、bit-perfect 输出、无缝播放或专业音频延迟。
- 标签写回的自动回读与原文件备份测试只覆盖 WAV；MP3/FLAC/M4A 等其他格式的写回应继续用副本实测。
- 未测试 DRM、损坏或非标准编码的每一种组合。
- 未进行万首曲库压测、长时间播放测试或跨机器兼容测试。
- 本次环境不允许读取完整进程树指标，因此不报告不可靠的内存 / CPU 数字。发布包小不代表已达成低内存指标。
- .NET Desktop Runtime 与 WebView2 Runtime 必须预先安装；此包不包含运行时。

自动检查只对工作目录中的测试副本进行写入，没有改动用户的真实音乐。
