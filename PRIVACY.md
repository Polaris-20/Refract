# 本地数据说明

折光没有应用账号、云端曲库或应用分析事件。界面加载随程序分发的本地内容，应用不主动上传音乐、封面、歌词或曲库。

## 保存了什么

- `library.json`：音乐的绝对路径、元数据、收藏、歌单、导入的歌词和封面索引。
- `covers/`：导入或选择的封面副本。
- `desktop-lyrics.json`：悬浮歌词的显示、锁定、字号和位置。
- `webview/`：WebView2 的本地用户数据，包括界面偏好、缓存等。

便携包默认把以上内容放在程序旁的 `data/`。移除 `portable.flag` 后改用 `%LOCALAPPDATA%/Refract/`，两者不会自动迁移。

导入音乐不会复制原音乐文件；导入歌词会保存正文副本。普通信息编辑只修改曲库；确认“写回音乐文件”后才修改原文件，并在原音乐旁留下备份。

## 备份、删除与反馈

退出程序后可备份数据目录。删除数据目录会清除本地曲库与设置，不会删除原音乐文件；音乐旁的标签写回备份也不会被清除。

曲库包含本地路径，歌词和封面可能涉及私人收藏。报告问题时请提供最小复现步骤，检查截图和日志中的个人信息，不要上传整个 `data/` 或未经授权的音乐。

WebView2 与 .NET 是独立运行时，其更新与诊断行为由相应运行时的设置和条款决定。本说明描述折光应用自身的行为。
